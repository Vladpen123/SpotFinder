[README](https://github.com/user-attachments/files/25312274/README.md)

🚗 SpotFinder
SpotFinder — это распределенная микросервисная система для поиска, бронирования и оплаты парковочных мест в реальном времени.
Проект построен на .NET 8/9 с использованием оркестратора .NET Aspire. Он демонстрирует современные подходы к построению High-Load систем: гео-поиск, распределенные блокировки, событийно-ориентированную архитектуру и gRPC взаимодействие.
🏗️ Архитектура и Технологии
Проект использует гибридную архитектуру хранения данных и асинхронное взаимодействие.
Стек технологий
* Платформа: .NET 10
* Оркестрация: .NET Aspire
* Фронтенд: Blazor Server (Interactive Mode)
* API & Коммуникация: gRPC (Protobuf), REST (для внешних клиентов через YARP)
* Брокер сообщений: Apache Kafka
* Базы данных:
   * Redis: Гео-индекс (GEOADD, GEORADIUS), горячие данные, распределенные блокировки (RedLock).
   * PostgreSQL: Хранение истории заказов (BookingService) и пользователей (Identity).
* Аутентификация: ASP.NET Core Identity + Google OAuth 2.0.
* Карты: Leaflet.js + OpenStreetMap.
🧩 Микросервисы
Сервис
	Ответственность
	Ключевые технологии
	SpotFinder.Web
	UI приложения. Интерактивная карта, кабинет владельца, история заказов. Выступает как BFF (Backend for Frontend).
	Blazor, SignalR, gRPC Client, Kafka Consumer
	SpotFinder.GeoService
	Управление точками на карте. Поиск в радиусе. Хранение статусов (Available, Reserved, Paid).
	Redis Geo, gRPC Server, Kafka Consumer
	SpotFinder.BookingService
	Создание бронирований, хранение истории. Управляет жизненным циклом заказа.
	PostgreSQL (EF Core), RedLock, gRPC Server
	SpotFinder.PaymentService
	Обработка платежей. Интеграция с Google Pay (Mock/Test).
	gRPC Server, Kafka Producer
	SpotFinder.AppHost
	Точка входа для запуска всех контейнеров и настройки связей.
	.NET Aspire
	🚀 Функциональность
1. Гео-поиск: Мгновенное отображение парковок в радиусе 10 км (Redis Geo).
2. Бронирование:
   * Защита от Race Condition (двойного бронирования) через RedLock.
   * Временная бронь на 10 минут (Redis TTL).
3. Оплата:
   * Интеграция с Google Pay (Test Environment).
   * Асинхронное подтверждение через Kafka (billing topic).
4. Real-time обновления:
   * Если кто-то забронировал или оплатил место, карта обновляется у всех пользователей без перезагрузки (Kafka -> Web Worker -> SignalR).
5. Личный кабинет:
   * Водитель: История поездок и трат.
   * Владелец: Управление своими парковками (добавление, удаление, просмотр статуса).
6. Фоновые задачи:
   * Автоматическая отмена неоплаченных броней (Cleanup Worker).
🛠️ Запуск проекта
Предварительные требования
* Docker Desktop (должен быть запущен).
* .NET 8.0 SDK или выше.
* Рабочий аккаунт Google Cloud (для получения ClientId/Secret).
1. Клонирование
git clone [https://github.com/your-username/SpotFinder.git](https://github.com/your-username/SpotFinder.git)
cd SpotFinder

2. Настройка секретов (Google Auth)
Проект использует User Secrets для хранения ключей Google. Вам нужно инициализировать их в проекте оркестратора SpotFinder.AppHost.
1. Получите Client ID и Client Secret в Google Cloud Console.
   * Redirect URI: https://localhost:{PORT}/signin-google (Порт веб-приложения можно найти после первого запуска или зафиксировать в launchSettings).
2. Выполните команды в терминале:
dotnet user-secrets init --project SpotFinder.AppHost/SpotFinder.AppHost.csproj
dotnet user-secrets set "Authentication:Google:ClientId" "ВАШ_CLIENT_ID" --project SpotFinder.AppHost/SpotFinder.AppHost.csproj
dotnet user-secrets set "Authentication:Google:ClientSecret" "ВАШ_SECRET" --project SpotFinder.AppHost/SpotFinder.AppHost.csproj

3. Запуск
Откройте решение в Visual Studio / Rider или запустите через терминал:
# Запуск оркестратора
dotnet run --project SpotFinder.AppHost/SpotFinder.AppHost.csproj

После запуска откроется .NET Aspire Dashboard, где вы увидите статус всех контейнеров (Redis, Postgres, Kafka) и микросервисов.
Нажмите на Endpoint проекта spotfinder-web, чтобы открыть приложение.
📦 Структура базы данных и событий
Kafka Topics
* bookings — События создания новой брони (для аналитики или уведомлений).
* billing — События успешной оплаты. Триггерит обновление статуса в GeoService (Redis) и BookingService (Postgres).
* spots — События создания новых парковочных мест.
Redis Keys
* spots_geo (Geo) — Гео-индекс всех точек.
* spot:{id} (Hash) — Детали точки (цена, владелец, статус).
* reservation:spot:{id} (String) — Ключ блокировки с TTL (10 мин).
* owner:{userId}:spots (Set) — Список ID парковок, принадлежащих пользователю.
🤝 Вклад в проект (Contributing)
1. Создайте Fork репозитория.
2. Сделайте Feature Branch (git checkout -b feature/AmazingFeature).
3. Закоммитьте изменения (git commit -m 'Add some AmazingFeature').
4. Запушьте в ветку (git push origin feature/AmazingFeature).
5. Откройте Pull Request.

